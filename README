專題方法說明：使用 Python 擷取亞洲大學資工系專任師資資料
本專案目的是透過 Python 撰寫爬蟲程式，從「亞洲大學資訊工程學系」的師資介紹頁面中擷取每位教授的姓名與研究領域資料，並可延伸儲存為結構化資料以利分析。
1. 所用方法與工具
本次使用的技術與工具如下：
程式語言： Python 3
主要套件：
  `requests`：用來發送 HTTP 請求並取得網頁內容。
  `BeautifulSoup`（bs4）：用來解析 HTML 結構並擷取指定資料。
資料來源：亞洲大學資訊工程學系官方網站  
  網址：https://csie.asia.edu.tw/zh_tw/TeacherIntroduction/Full_time_faculty
2. 操作流程與原理
整體爬蟲流程可分為以下幾個步驟：
（1）發送請求取得網頁內容 
利用 `requests.get()` 函式對目標網址發送 GET 請求，並加入 `User-Agent` 標頭，模擬瀏覽器行為，避免被網站阻擋。

（2）解析 HTML 結構
收到的 HTML 內容會使用 `BeautifulSoup` 做解析，將網頁轉換為可以操作的資料結構，方便進一步搜尋。

（3）擷取資料 
每位教授的資料都包含在 `<ul>` 標籤中，class 為 `i-member-profile-list`。  
在每個區塊中，我們透過指定的 class 名稱擷取出：

教授姓名（class 為 `member-data-value-name`）
研究領域（class 為 `member-data-value-7`）

這些資料會用 `.get_text(strip=True)` 方法轉成乾淨的純文字。

（4）資料顯示或儲存
資料可以直接印出顯示，也可進一步儲存為 CSV 檔案，方便後續處理與分析。

3. 優點與應用
✅ 可快速擷取網站上的結構化公開資料。
✅ 程式簡單、可重複使用。
✅ 可進一步擴充功能，如抓取分機、職稱、辦公室位置等欄位。
✅ 適合製作師資介紹資料表、學術統計資料分析或系所網頁輔助工具。
